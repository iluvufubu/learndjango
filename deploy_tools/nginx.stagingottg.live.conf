server {
    listen 80;
    server_name stagingottg.live;

    location /static {
        alias /home/rudy3/sites/stagingottg.live/static;
    }

    location / {
        proxy_pass http://unix:/tmp/stagingottg.live.socket;
        proxy_set_header Host $host;
    }
}